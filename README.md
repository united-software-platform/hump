# Hump

[![версия SDD Developer Kit из .sdd-kit-manifest.json](https://img.shields.io/badge/dynamic/regex?url=https%3A%2F%2Fraw.githubusercontent.com%2Funited-software-platform%2FHump%2Fmain%2F.sdd-kit-manifest.json&search=%22kit_version%22%3A%5Cs%2A%22%28%5B%5Cd.%5D%2B%29%22&replace=v%241&label=sdd%20developer%20kit&color=5B4FCF&logo=docker&logoColor=white)](https://github.com/united-software-platform/sdd-developer-kit/pkgs/container/sdd-developer-kit)
[![LiteLLM](https://img.shields.io/badge/LiteLLM-LLM%20API-1DA1F2)](https://www.litellm.ai/)
[![Ollama](https://img.shields.io/badge/Ollama-локальные%20модели-000000?logo=ollama&logoColor=white)](https://ollama.com/)

Hump — интеллектуальный маршрутизатор LLM-запросов: единая точка входа, которая принимает запросы
от систем и распределяет их между доступными моделями и AI-провайдерами.

---

## Навигация

- [Почему «Сортировочная горка»](#почему-сортировочная-горка)
- [Проблема](#проблема)
- [Цель](#цель)
- [Задачи](#задачи)
- [Состав](#состав)
- [Запуск шлюза](#запуск-шлюза)
- [Панель](#панель)
- [Данные шлюза](#данные-шлюза)
- [Проверка](#проверка)
- [Статус](#статус)

---

## Почему «Сортировочная горка»

Сортировочная горка — железнодорожное сооружение для расформирования составов. Прибывший поезд
надвигают на вершину насыпи, вагоны по одному скатываются вниз, и стрелки переводят каждый из них
на тот путь, который отвечает его назначению. Один входной путь, десятки выходных, и решение
принимается отдельно по каждому вагону.

Hump (англ. «горка») делает то же самое с запросами. Системы отправляют их в одну точку, а
маршрутизатор для каждого запроса выбирает, куда его направить: к какой модели и какому провайдеру —
исходя из типа задачи, требований запроса и доступных в этот момент ресурсов.

---

## Проблема

Каждая система, которой нужна LLM, решает одни и те же вопросы заново:

- к какому провайдеру идти и по какому протоколу — у облачных API и локальных моделей они разные;
- какую модель выбрать под конкретную задачу — дешёвую и быструю или тяжёлую и точную;
- что делать при недоступности провайдера, исчерпании лимитов и ошибках;
- где держать ключи доступа и как считать расход.

В результате одна и та же логика дублируется в Garden, Experience Mine и остальных системах, смена
модели или провайдера требует правок в каждой из них, а единой картины того, сколько запросов и куда
уходит, нет ни у кого.

---

## Цель

Снять выбор модели и провайдера с систем-потребителей. Система формулирует, что ей нужно, и
отправляет запрос в Hump; решение о маршруте принимает Hump.

---

## Задачи

- Принимать запросы по единому интерфейсу, не зависящему от конкретного провайдера.
- Выбирать модель и провайдера под каждый запрос: по типу задачи, требованиям к качеству, скорости
  и стоимости, а также по доступности и загрузке ресурсов.
- Поддерживать и облачные API, и локальные модели — в том числе сценарии, где данные не должны
  покидать собственную инфраструктуру.
- Переживать отказы провайдеров: повторять запрос и переключаться на запасной маршрут.
- Вести единый учёт обращений и расхода в разрезе систем и моделей.

---

## Состав

- [LiteLLM](https://www.litellm.ai/) — единый API-слой поверх LLM-провайдеров: приводит облачные
  сервисы разных вендоров к одному интерфейсу, поэтому Hump работает с ними единообразно и не
  зависит от протокола конкретного вендора.
- [Ollama](https://ollama.com/) — запуск локальных моделей: даёт маршрутизатору направление,
  в котором запрос обрабатывается на собственной инфраструктуре, без обращения к внешним API.

---

## Запуск шлюза

Окружение описано в [`docker-compose.hump.yml`](./docker-compose.hump.yml) и состоит из четырёх
сервисов: `litellm` — сам шлюз, `postgres` — хранилище его состояния, `ollama` — сервер локальных
моделей, `ollama-pull` — разовая загрузка весов при запуске. Маршруты заданы в [`config/litellm.yaml`](./config/litellm.yaml):
`hump-local` — модель в Ollama, `hump-cloud` — модель облачного провайдера.

Предусловия:

- Docker с плагином Compose на хосте;
- действующий ключ облачного провайдера;
- свободное место под веса модели — первый запуск скачивает их в `data/ollama-models/`.

Заполнить в `.env` переменные из блока `LiteLLM gateway` файла [`.env.example`](./.env.example):

| Переменная | Назначение | Обязательна |
|------------|------------|-------------|
| `LITELLM_MASTER_KEY` | Административный ключ шлюза, начинается с `sk-` | да |
| `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB` | Учётные данные и имя базы, в которой шлюз хранит своё состояние | да |
| `DATABASE_URL` | Строка подключения шлюза к той же базе | да |
| `UI_USERNAME`, `UI_PASSWORD` | Вход в панель; пока пусты, действует `admin` с паролем `LITELLM_MASTER_KEY` | нет |
| `ANTHROPIC_BASE_URL`, `ANTHROPIC_AUTH_TOKEN` | Адрес и ключ облачного маршрута | для `hump-cloud` |
| `LITELLM_SALT_KEY` | Шифрование ключей провайдеров, когда шлюз хранит их в базе | нет |

`DATABASE_URL` и три переменные `POSTGRES_*` описывают одну и ту же базу: пароль и имя базы
в строке подключения должны совпадать с ними дословно. Это единственное место двойной правки
в настройке шлюза — при расхождении шлюз не проходит авторизацию в базе при старте.

Тег образа шлюза стоит закрепить в `LITELLM_IMAGE_TAG` вместо `latest`: схему базы мигрирует сам
образ при старте, и смена версии меняет схему уже заполненных данных.

За облачным маршрутом стоит не обязательно Anthropic: `anthropic` в конфигурации означает
протокол, а адрес сервиса задаёт `ANTHROPIC_BASE_URL` — подойдёт любой сервис с совместимым API.

Окружение агента (`claude`) живёт в отдельном файле `.env.claude`, созданном из
[`.env.claude.example`](./.env.claude.example): там только переменные агента. Имена `ANTHROPIC_*`
зарезервированы самим Claude Code, поэтому из `.env` их в окружение агента не переносят — иначе
отключается claude.ai-логин и коннекторы организации. Переменные продукта Hump остаются в `.env`.

Поднять окружение:

```bash
make hump-up
```

После запуска шлюз принимает OpenAI-совместимые запросы на `http://localhost:4000`, обращения
к API моделей требуют ключ `LITELLM_MASTER_KEY`. Состояние и логи — `make hump-logs`,
остановка — `make hump-down`.

Ollama работает на процессоре и порт наружу не публикует: к нему обращается только шлюз
по внутренней сети. Чтобы увести локальный маршрут на внешний Ollama, достаточно задать
`OLLAMA_API_BASE` в `.env`.

Смена локальной модели затрагивает два места: имя в `config/litellm.yaml` и команду сервиса
`ollama-pull` в `docker-compose.hump.yml` — значения должны совпадать.

> **Внимание:** ключи задаются только в `.env` — он исключён из репозитория. `config/litellm.yaml`
> ссылается на имена переменных окружения и значений ключей не содержит.

---

## Панель

Административная панель шлюза открывается по адресу `http://localhost:4000/ui`. Вход — значения
`UI_USERNAME` и `UI_PASSWORD` из `.env`; пока они не заполнены, действует пользователь `admin`
с паролем, равным `LITELLM_MASTER_KEY`.

> **Внимание:** вход мастер-ключом означает, что панель открывается тем же секретом, что и API
> моделей. Для окружения, доступного не только автору, `UI_USERNAME` и `UI_PASSWORD` задаются
> отдельными значениями.

Панель показывает настроенные маршруты, обращения к моделям и расход, а также выдаёт ключи доступа
отдельным системам-потребителям. Такой ключ работает наравне с мастер-ключом при обращении
к API моделей, но принадлежит одной системе: обращения по нему различимы в общем потоке, а отзыв
ключа закрывает доступ только этой системе.

Порядок выдачи: в разделе ключей панели создать ключ, выбрать доступные ему маршруты и передать
значение системе-потребителю. Мастер-ключ остаётся административным и системам не раздаётся.

Панель работает только с подключённой базой: без неё шлюз отвечает на вход ошибкой
`Authentication Error, Not connected to DB!`.

---

## Данные шлюза

Данные сервисов лежат в каталоге `data/` внутри проекта, а не в томах Docker: их видно без
`docker volume`, можно скопировать и забрать в резервную копию обычными средствами.

| Каталог | Содержимое | Кто заполняет |
|---------|------------|---------------|
| `data/postgres/` | Состояние шлюза: выданные ключи и записи об обращениях | база, от имени пользователя `postgres` из образа |
| `data/ollama-models/` | Веса локальных моделей | сервис `ollama-pull` при запуске |

Каталог `data/` исключён из репозитория и создаётся целью `make init`. Содержимое переживает
`make hump-down` и пересоздание контейнеров.

> **Внимание:** потеря `data/postgres/` означает отзыв доступа у всех систем-потребителей сразу —
> выданные ключи восстановлению не подлежат. Резервное копирование в состав проекта не входит
> и остаётся обязанностью эксплуатации.

`data/postgres/` принадлежит пользователю базы из образа, а не владельцу репозитория: читать
и править его содержимое напрямую не следует — этим занимается сама база.

Записи об обращениях копятся, и каталог растёт: автоматической ротации нет. Удалить накопленное
можно только вместе с каталогом, остановив окружение:

```bash
make hump-down
sudo rm -rf data/postgres
make hump-up
```

`sudo` нужен по той же причине: каталог создан не пользователем проекта. Следующий запуск
разворачивает базу заново — пустой, без выданных ключей.

Порт базы наружу не публикуется: к ней обращается только шлюз по внутренней сети.

---

## Проверка

```bash
make hump-check
```

Цель читает ключи из `.env` и запускает [`tools/litellm/check.sh`](./tools/litellm/check.sh):
скрипт проверяет живость и готовность шлюза, подключение базы, состояние настроенных маршрутов
и отправляет по одному реальному запросу на каждый из них. Коды возврата: `0` — база подключена
и все маршруты отвечают, `1` — база не подключена или хотя бы один маршрут не ответил,
`2` — ошибка вызова или окружения.

Скрипт можно вызвать и напрямую — он сам выбирает доступный HTTP-клиент (`curl`, иначе `wget`),
а адрес шлюза берёт из `HUMP_GATEWAY_URL`:

```bash
HUMP_GATEWAY_URL=http://localhost:4000 LITELLM_MASTER_KEY=sk-... sh tools/litellm/check.sh
```

Docker'а в контейнере агента нет, поэтому окружением он управляет целями `make` через
[host-runner](./tools/host-runner): разрешённые цели перечислены в его белом списке, а мастер-ключ
остаётся на хосте — цель `hump-check` читает `.env` сама.

---

## Статус

Работает базовая точка входа: запрос по OpenAI-совместимому API доходит и до локальной, и до
облачной модели. Работает панель: маршруты, обращения и расход видны, системам-потребителям
выдаются отдельные ключи. Собственной логики маршрутизации Hump пока нет — выбор маршрута делает
система-потребитель, называя имя модели, а распределение задаёт конфигурация LiteLLM.

Задачи из одноимённого раздела — выбор модели по типу задачи и требованиям, переключение на
запасной маршрут при отказе, единый учёт расхода — предмет следующих изменений.
