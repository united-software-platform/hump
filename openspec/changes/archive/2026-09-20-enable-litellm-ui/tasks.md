## 1. Хранилище состояния

- [x] 1.1 Добавить в `docker-compose.hump.yml` сервис PostgreSQL с именованным томом под данные
  и без публикации порта наружу; проверка — `make hump-up` поднимает сервис, `make hump-logs`
  показывает его в состоянии `running`
- [x] 1.2 Добавить сервису базы healthcheck готовности принимать соединения; проверка — в выводе
  `docker compose -f docker-compose.hump.yml ps` сервис получает состояние `healthy`
- [x] 1.3 Связать шлюз с базой: зависимость с ожиданием готовности по решению 3 `design.md`;
  проверка — при холодном старте шлюз не пишет в лог отказов подключения к базе
- [x] 1.4 Добавить в `.env.example` переменные сервиса базы и строку `DATABASE_URL` целиком,
  с комментарием о том, что значения обоих мест должны совпадать; проверка — значения примера
  согласованы между собой, реальные секреты в файл не попали

## 2. Вход в панель

- [x] 2.1 Добавить в `.env.example` переменные `UI_USERNAME` и `UI_PASSWORD` с пояснением, что
  до их заполнения действует вход `admin` с паролем, равным `LITELLM_MASTER_KEY`; проверка —
  пояснение совпадает с поведением, наблюдаемым на странице входа
- [x] 2.2 Проверить вход в панель заданными учётными данными; проверка — панель открывается
  и показывает маршруты `hump-local` и `hump-cloud`
- [x] 2.3 Проверить отказ входа при неверных учётных данных; проверка — панель не открывается,
  данные маршрутов и ключей не показываются

## 3. Ключи систем-потребителей

- [x] 3.1 Выдать через панель ключ тестовой системе-потребителю и выполнить им запрос
  `POST /v1/chat/completions`; проверка — запрос обслуживается, обращение видно в панели
  за этим ключом
- [x] 3.2 Отозвать выданный ключ; проверка — запрос с отозванным ключом отклоняется, запрос
  с мастер-ключом по-прежнему обслуживается
- [x] 3.3 Проверить, что состояние переживает перезапуск: `make hump-down`, затем `make hump-up`;
  проверка — ранее выданный действующий ключ работает, записи о прежних обращениях видны в панели

## 4. Проверка окружения

- [x] 4.1 Дополнить `tools/litellm/check.sh` разбором поля `db` ответа `/health/readiness`
  по решению 5 `design.md`; проверка — при поднятой базе шаг готовности сообщает о подключении
- [x] 4.2 Проверить поведение при недоступной базе: остановить сервис базы и выполнить
  `make hump-check`; проверка — шаг готовности сообщает, что хранилище не подключено, скрипт
  завершается кодом `1`
- [x] 4.3 Убедиться, что коды возврата скрипта остались прежними (`0`, `1`, `2`) и его заголовочный
  комментарий им соответствует; проверка — успешный прогон при поднятой базе даёт код `0`

## 5. Документация

- [x] 5.1 Переписать раздел README о панели: адрес, вход, назначение панели и порядок выдачи ключа
  системе-потребителю взамен формулировки об ограничении из изменения `revise-readme`; проверка —
  в README не осталось упоминаний ошибки `Not connected to DB!`
- [x] 5.2 Дополнить таблицу переменных README новыми переменными базы и панели; проверка —
  множество имён переменных таблицы совпадает с `.env.example`
- [x] 5.3 Зафиксировать в README эксплуатационные последствия: рост тома с данными, команда его
  очистки, обязанность резервного копирования, закрепление тега образа шлюза вместо `latest`;
  проверка — каждый пункт подтверждается соответствующей строкой `docker-compose.hump.yml`
  или `.env.example`

## 6. Данные сервисов в каталоге проекта

- [x] 6.1 Заменить именованные тома `postgres-data` и `ollama-models` на каталоги `./data/postgres`
  и `./data/ollama-models`, задать базе `PGDATA` в подкаталоге точки монтирования; проверка —
  секции `volumes` верхнего уровня в compose-файле не осталось
- [x] 6.2 Исключить `data/` из репозитория и добавить его в перечень цели `init-gitignore`;
  проверка — `git status` не показывает содержимое `data/`
- [x] 6.3 Создавать каталоги данных в `init-dirs`, чтобы владельцем стал пользователь проекта,
  а не `root`; проверка — секции `Makefile` остаются в норме 30 строк по `MAKE-001`
- [x] 6.4 Описать в README каталоги данных, их владельца и порядок очистки; проверка — раздел
  «Данные шлюза» не упоминает томов Docker
- [x] 6.5 Поднять окружение на каталогах проекта и убедиться, что база разворачивается, а веса
  загружаются; проверка — `make hump-check` завершается кодом `0`, `data/postgres/pgdata`
  и `data/ollama-models` непусты
