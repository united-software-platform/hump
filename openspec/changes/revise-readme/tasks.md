## 1. Сверка фактов перед правкой

- [ ] 1.1 Выписать из `Makefile` актуальный состав целей `init`, `init-env`, `hump-up`, `hump-down`,
  `hump-logs`, `hump-check` с их описаниями; проверка — перечень совпадает с выводом `make help`
- [ ] 1.2 Выписать из `.env.example` и `.env.claude.example` полный состав переменных с умолчаниями;
  проверка — ни одна переменная примеров не осталась вне выписки
- [ ] 1.3 Сверить имена маршрутов и моделей в `config/litellm.yaml` с командой сервиса `ollama-pull`
  в `docker-compose.hump.yml`; проверка — имя локальной модели в обоих файлах совпадает, расхождение
  зафиксировано в тексте README как место двойной правки
- [ ] 1.4 Зафиксировать состояние панели: запрос `/health/readiness` возвращает `"db": "Not connected"`,
  вход в `/ui` завершается `Authentication Error, Not connected to DB!`; проверка — формулировка
  раздела о панели опирается на этот вывод, а не на предположение

## 2. Переработка README

- [ ] 2.1 Перестроить навигацию и порядок разделов по решению 1 `design.md`; проверка — пункты
  навигации совпадают с заголовками файла один к одному и идут в том же порядке
- [ ] 2.2 Написать вводный блок: назначение Hump на 3–5 строк сразу под блоком бейджей; проверка —
  читателю ясно, что это шлюз LLM-запросов, до первого подзаголовка
- [ ] 2.3 Написать раздел «Предусловия»: Docker с плагином Compose, ключ облачного провайдера, место
  под веса модели; проверка — каждый пункт связан с конкретным сервисом `docker-compose.hump.yml`
- [ ] 2.4 Написать раздел «Развёртывание»: `make init` → заполнение `.env` → `make hump-up`, с явным
  указанием, что `make init` не перезаписывает существующие `.env` и `.env.claude`; проверка — команды
  дословно совпадают с целями `Makefile` из задачи 1.1
- [ ] 2.5 Написать раздел «Настройка» с таблицей переменных шлюза (`.env`) по решению 2: переменная,
  назначение, обязательность, умолчание или пример; проверка — таблица покрывает все переменные блока
  `LiteLLM gateway` из задачи 1.2
- [ ] 2.6 Дополнить раздел «Настройка» таблицей переменных агента (`.env.claude`) и объяснением,
  почему `ANTHROPIC_*` не переносятся в окружение агента; проверка — объяснение сохраняет смысл
  комментария из `.env.claude.example`
- [ ] 2.7 Написать раздел «Обращение к API»: адрес `http://localhost:4000`, пример
  `POST /v1/chat/completions` с `hump-local` и указание на замену имени модели на `hump-cloud`;
  проверка — пример повторяет формат запроса из `tools/litellm/check.sh`
- [ ] 2.8 Написать раздел «Панель LiteLLM» по решению 3: адрес `/ui`, учётные данные по умолчанию,
  ограничение входа без БД и отсылка к изменению `enable-litellm-ui`; проверка — ограничение названо
  дословной формулировкой ошибки из задачи 1.4
- [ ] 2.9 Переписать раздел «Проверка»: `make hump-check`, что именно проверяет скрипт, коды возврата
  и прямой вызов с `HUMP_GATEWAY_URL`; проверка — коды возврата совпадают с заголовком
  `tools/litellm/check.sh`
- [ ] 2.10 Написать раздел «Эксплуатация»: `make hump-logs`, `make hump-down`, смена локальной модели
  в двух местах, внешний Ollama через `OLLAMA_API_BASE`, работа через host-runner из контейнера
  агента; проверка — каждый пункт подтверждается соответствующим файлом репозитория
- [ ] 2.11 Сжать повествовательную часть (название, проблема, цель, задачи) и перенести её под
  инструкцию; проверка — смысл метафоры и связка «проблема — цель — задачи» сохранены, объём
  сокращён примерно вдвое
- [ ] 2.12 Обновить разделы «Состав» и «Статус»: состояние панели и отсутствие собственной логики
  маршрутизации; проверка — в «Статусе» нет утверждений о ещё не реализованном поведении

## 3. Синхронизация примеров окружения

- [ ] 3.1 Привести комментарии блока `LiteLLM gateway` в `.env.example` к однострочному назначению
  каждой переменной; проверка — значения переменных не изменены, `git diff` затрагивает только
  строки комментариев
- [ ] 3.2 Сверить таблицы README с `.env.example` и `.env.claude.example`; проверка — множества имён
  переменных совпадают в обе стороны, расхождений нет

## 4. Проверка результата

- [ ] 4.1 Проверить соответствие README правилам документации: структура по `DOC-014`, иерархия
  заголовков по `DOC-012`, отсутствие HTML по `DOC-005`, отсутствие таблицы версий по `DOC-022`,
  абсолютные ссылки только на внешние цели по `DOC-015`; проверка — каждое правило просмотрено
  по тексту файла
- [ ] 4.2 Проверить работоспособность всех относительных ссылок README; проверка — каждый путь
  существует в репозитории
- [ ] 4.3 Пройти README как инструкцию с чистого клона на хосте: `make init`, заполнение `.env`,
  `make hump-up`, `make hump-check`; проверка — `make hump-check` завершается кодом `0`, шагов,
  не описанных в README, не потребовалось
